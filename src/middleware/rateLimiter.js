// src/middleware/rateLimiter.js
// Express Middleware Wrapper — Implemented later (Option C bonus)
//
// Stub placeholder — see d9 tasks in the plan.

module.exports = (config) => (req, res, next) => next();
